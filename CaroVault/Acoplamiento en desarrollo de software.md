El acoplamiento es el grado de conexión o dependencia que existe entre diferentes partes de un programa. Un diseño con bajo acoplamiento permite que los módulos funcionen de forma más independiente, facilitando que el sistema sea más fácil de mantener, modificar y reutilizar sin afectar otras partes del código.

